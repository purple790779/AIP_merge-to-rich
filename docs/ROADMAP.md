# 라이브 운영 로드맵

상태: Planned  
기준일: 2026-03-10

## 1. 현재 운영 상태
- 플레이스토어 등록 및 라이브 운영 단계
- 일일 보상 / 복귀 보상 / 오프라인 보상 구현 완료
- 미션 18개 / 업적 108개 / 랭크 메타 반영
- timed reward 보류 / 재오픈 UX 반영
- 현재 지역 배너 + 지역 선택 / 해금 UI 1차 반영
- 지역 해금 / 선택의 실제 진입점은 `RegionModal`, 상점은 `다음 지역 목표` 안내 중심
- 일일 보상은 `다음 KST 리셋 / streak cap / clock rollback 잠금` 안내까지 반영된 상태
- 미션 현금 보상은 초중반 pacing을 살리고 late milestone 급팽창을 막는 구간형 scaling 기준으로 정리됨
- `2026-03-10` morning QA pass 기준 fresh install 후 `npm run lint`, `npm run build` 통과
- 광고 SDK / IAP는 아직 미연동

## 2. 다음 우선순위

### 1순위: 플레이타임 확장 완성
- `무한의농장` 벤치마킹을 기반으로 단일 보드 구조를 `지역 확장형 구조`로 전환
- `시티 뱅크 -> 골드 익스체인지 -> 젬 마켓 -> 스페이스 볼트` 순서의 월드 구조 유지
- 실제 지역별 보드 / 생산 계열 분리 범위 확정
- 보드 deadlock rescue 장치 추가
- 지역 전환 시 시각적 테마 / 경제 목표 / 콘텐츠 기대가 실제 플레이 차이로 이어지게 설계

### 2순위: 경제 리밸런싱 2차
- `v1.5.5` 기준 1차 완료
  - 수익 주기 업그레이드 체감/비용 재조정
  - 머지 보너스 기대값 상향
  - 미션 / 업적 현금 보상 비중 축소
  - 시작 레벨 업그레이드의 보드 삭제/환급 구조 제거
- 다음 단계
  - 복귀 / 오프라인 보상 수치 재조정
  - 지역 해금 비용과 메인 경제 곡선 연결
  - 지역별 보드 분리 이후 초반/중반/후반 성장 시간을 다시 계측
  - 이번 배치에서 정리한 `일일 보상 status snapshot`과 `mission reward band` 기준이 실제 플레이타임과 맞는지 계측
- 현금 보상 일부를 `해금 토큰 / 부스트 티켓 / 광고 배수` 같은 보상으로 치환 검토

### 3순위: 리텐션 고도화
- 주간 / 이벤트형 미션 확장
- 복귀 / 오프라인 보상 수치 보정
- 컬렉션과 지역 메타 연결

### 4순위: 수익화 준비
- 보상형 광고 연결 포인트 정리
  - 오프라인 보상 2배
  - 복귀 보상 추가 지급
  - 한정 시간 부스트
- 스토어 문구 / 정책 문서 / QA 체크리스트 정리

### 5순위: QA / 운영 하드닝
- 짧은 세로 화면 레이아웃 점검
- 문자열 전수 감사
- timed reward 중복 수령 / 폐기 / 재오픈 흐름 점검
- `npm audit` 취약점 8건 분류 및 업그레이드 가능 범위 검토

## 3. 다음 세션 추천 작업
1. deadlock rescue 시스템 초안 + region board 분리 경계 정의
2. 복귀 / 오프라인 보상 수치 재조정
3. 지역별 생산 계열 설계
4. 문자열 / 좁은 화면 / timed reward / daily reward smoke QA

## 4. 다음 버전 전 검증 게이트
- Node `22.x` fresh install 기준 `npm run lint`, `npm run build`
- 지역 해금/선택 smoke test
- timed reward pending 복원/재오픈/폐기 흐름
- daily reward 자정 reset / clock rollback guard / 7일 cap 문구 점검
- 좁은 화면에서 헤더/배너/하단 메뉴 겹침 여부
